


# import magpylib as magpy
# cube = magpy.magnet.Cuboid(
#     polarization=(0,0,1),
#     dimension=(3,3,3),
# )
# cube.mesh = (1,1,2)
# mesh = mesh_cuboid(cube)

# print(mesh)

# import matplotlib.pyplot as plt
# ax = plt.figure().add_subplot(projection='3d')


# center = cube.position
# length, width, height = cube.dimension
# X, Y, Z = cuboid_data(center, (length, width, height))
# ax.plot_surface(np.asarray(X), np.asarray(Y), np.asarray(Z), color='b', rstride=1, cstride=1, alpha=0.1)
# ax.plot(mesh[:,0], mesh[:,1], mesh[:,2], ls='', marker='o')
# ax.set_aspect('equal')
# plt.show()
